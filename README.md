# Student-Information-System-DACILLOxRAMOS
The Student Information System (SIS) is a desktop application built with Python 3, PyQt5 for the graphical user interface, and SQLite for local database storage. The system allows school administrators to manage student records through a clean, modern GUI — supporting all CRUD operations without any external database server.
